// 関数greeting は "hello!world!"など、任意の文字列返す関数です。 引数valueに型注釈してください
// const greeting = (value) => "hello!" + value;

const greeting = (value: string) => "hello!" + value;

console.log(greeting("Sae"));
